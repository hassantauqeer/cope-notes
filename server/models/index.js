const User = require('./User')
const Message = require('./Message')
const UserMessage = require('./UserMessage')
const Configs = require('./Config')

module.exports = {User, UserMessage, Message, Configs};
